@model Infrastructure.Models.PlanesCobro

@{
    ViewBag.Title = "Details";
}

<h2>Detalles</h2>

<div>
    <h4>PlanesCobro</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.EstadoPlanesCobro.Descripcion)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.EstadoPlanesCobro.Descripcion)
        </dd>

    </dl>

    <ul>
        @foreach (var item in Model.RubroCobro)
        {
            <li>
                @item.Descripcion - @item.Monto
            </li>
        }

    </ul>

</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.ID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
